namespace CarrierDomain.Models
{
    public enum Role
    {
        Admin, Employee, Employer
    }
    public enum UserType
    {
        Employee, Employer
    }
    public enum Gender
    {
        Male, Female, Other
    }
}